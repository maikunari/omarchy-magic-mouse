[Unit]
Description=Mac-like Magic Mouse daemon (omarchy-magic-mouse)
After=graphical-session.target

[Service]
ExecStart=%h/.local/bin/magic-mouse-daemon
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
